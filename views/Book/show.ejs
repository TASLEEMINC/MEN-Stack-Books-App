<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Book Show Page</title>
  </head>
  <body>
    <h1>Book Show Page</h1>
    <h2>Name: <%=Book.name %></h2>

    <% if (Book.isRead) { %>
    <p>This Book is read!</p>
    <% } else { %>
    <p>This Book is not read!</p>
    <% } %>

    <!-- form -> method=POST action=/Books/<id>?_method=DELETE -->
    <!-- action attribute - directs the request to the URL path provided -->
    <!-- set type to method so methodOverride will check the request -->
    <!-- the endpoint for the request includes a query param string ?_method= -->
    <!-- the value provided with _method query param is DELETE -->
    <!-- methodOverride will automatically convert the POST to a DELETE before passing it to the correct routes -->

    <form method="POST" action="/Book/<%= Book._id%>?_method=DELETE">
      <button type="submit">Delete Book</button>
    </form>

    <!-- TODO - add a link to the edit - Book/:id/edit -->
    <section>
      <a href="/Book/<%= Book._id%>/edit">Edit Book</a>
    </section>
  </body>
</html>