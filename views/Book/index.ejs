<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Books Index Page</title>
</head>

<body>
  <%- include('../partials/nav') %>
    <h1><%= message %> - All Books</h1>
    <ul>
        <!-- EJS iterator -> loops over the Books context variable - set inside of res.render( {}) -->
      <% Book.forEach(Book => { %>
        <li><a href="/Book/<%= Book._id %>"><%= Book.name %></a></li>
      <% }); %>
    </ul>

    <a href="/">Go Back to Homepage</a> | <a href="/Book/new">Add new Book</a>
  </body>
  
</html>